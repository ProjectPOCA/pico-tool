/** Minimal electron stub for unit tests running under plain node. */
export const app = undefined as unknown as { isPackaged: boolean; getPath(name: string): string }
export const ipcMain = undefined
export const BrowserWindow = undefined
export const dialog = undefined
export const shell = undefined
